// Database
const user = require('../models/userModel')

const register = async (req, res) => {
    try {
        const { name, emsil, password } = req.body;
    } catch (err) {

    }
}

module.exports = {
    register
}